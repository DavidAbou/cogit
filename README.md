# cogit
Git tool for conventional commits

### Installation
Run:
```bash
$> sudo ./install.sh
```
Once done, your cogit is ready to be used.

#### Uninstall
Run:
```bash
$> sudo rm /usr/bin/cogit
```
